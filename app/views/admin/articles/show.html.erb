<h2 class="article_title"><%=@article.name%></h2>
<p class="pull-right">
  <span>由<%= @article.user.try(:nick_name) || @article.user.try(:email)%>创建于<%=custome_time_ago(@article.created_at)%></span>
</p>
<div class="row">
  <div class="col-md-10 col-sm-offset-1">
    <%=raw @article.content_html%>
  </div>
</div>
