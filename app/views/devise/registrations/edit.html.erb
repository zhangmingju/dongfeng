<div class="panel panel-default">
  <div class="panel-heading login">
    <h2>修改密码</h2>
  </div>
  <div class="panel-body">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= devise_error_messages! %>
      <% if flash[:alert].present?%>
        <div class="form-group form_padding">
          <div class="col-md-6 col-md-offset-3">
            <p class="alert alert-warning"><%=flash[:alert]%></p>
          </div>
        </div>
      <% end %>
      <div class="form-group form_padding">
        <%= f.label :current_password ,"原始密码", class:"col-md-4 col-md-offset-1 control-label"%>
        <div class="col-md-4">
          <%= f.password_field :current_password, autocomplete: "off", class:"form-control" %>
        </div>
      </div>
      
      <div class="form-group form_padding">
        <%= f.label :password ,"密码", class:"col-md-4 col-md-offset-1 control-label"%>
        <div class="col-md-4">
          <%= f.password_field :password, autocomplete: "off", class:"form-control" %>
        </div>
      </div>

      <div class="form-group form_padding">
        <%= f.label :password_confirmation,"确认密码", class:"col-md-4 col-md-offset-1 control-label" %>
        <div class="col-md-4">
          <%= f.password_field :password_confirmation, autocomplete: "off", class:"form-control" %>
        </div>
      </div>

      <div class="form-group form_padding">
        <div class="col-md-2 col-md-offset-5">
          <%= f.submit "确认",class: 'form-control btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>


