# encoding: utf-8
<p><=t('.welcome')%> <%= @email %>!</p>

<p><=t('.complete_sign_up')%></p>

<p><%= link_to t('.click_confirm'), confirmation_url(@resource, confirmation_token: @token) %></p>

<p><=t('.tip_info')%></p>
